# 绘画笔记项目说明

> 本文档记录项目的完整结构、各文件作用、内容格式规范以及日常维护流程。

---

## 技术栈

- **静态站点生成器**：MkDocs + Material for MkDocs 主题
- **部署平台**：GitHub Pages（`gh-pages` 分支自动构建）
- **仓库地址**：`https://github.com/betty-momo/drawing-notes`
- **访问地址**：`https://betty-momo.github.io/drawing-notes/`

---

## 项目目录结构

```
绘画笔记/
├── mkdocs.yml                  # 站点配置（核心文件）
├── .gitignore                  # 忽略 site/ 构建目录
├── 项目说明.md                  # 本文件
├── docs/                       # 所有内容源文件
│   ├── index.md                # 首页（卡片导航 + 课程资源）
│   ├── stylesheets/
│   │   └── extra.css           # 自定义样式（配色/排版/响应式/暗色模式）
│   ├── img/                    # 图片资源目录
│   │   ├── 火柴人.jpg
│   │   ├── 体块概括.png
│   │   ├── 眼睛.png
│   │   ├── 螳螂鬓.png
│   │   ├── 色相.png
│   │   └── ... (共 17 张)
│   ├── 人体知识.md              # 基础知识
│   ├── 场景知识.md
│   ├── 色彩知识.md
│   ├── 流程/                   # 绘画流程（按步骤排列）
│   │   ├── 起草.md
│   │   ├── 勾线.md
│   │   ├── 固有色.md
│   │   ├── 二分.md
│   │   ├── 三分.md
│   │   ├── 闭塞.md
│   │   ├── 灰面.md
│   │   ├── 高光.md
│   │   ├── 反光.md             # 占位，待补充
│   │   ├── 塑造.md             # 占位，待补充
│   │   └── 光源衰减.md         # 占位，待补充
│   └── 专题/                   # 专题深入
│       ├── 眼睛上色.md
│       ├── 头发塑造.md
│       ├── 皮肤塑造.md
│       ├── 衣褶.md
│       └── 泳装衣褶.md
└── site/                       # 构建输出（不提交，.gitignore 已忽略）
```

---

## 各文件作用详解

### 配置文件

| 文件 | 作用 |
|:------|:------|
| `mkdocs.yml` | 站点全局配置：站点名称、主题配色、导航结构、Markdown 扩展、搜索功能等 |
| `.gitignore` | 忽略 `site/` 构建输出目录 |

### 样式文件

| 文件 | 作用 |
|:------|:------|
| `docs/stylesheets/extra.css` | 完整的自定义 CSS 方案（约 730 行），包括：自定义紫蓝配色变量、标题样式（h1 渐变 / h2 左边框 / h3 圆点标记）、图片样式（圆角阴影 + 悬停放大）、表格样式（渐变表头 + 斑马纹）、首页卡片布局、场景知识透视卡片、手机端响应式适配、暗色模式适配、打印样式 |

### 内容页面

#### 首页

| 文件 | 作用 |
|:------|:------|
| `docs/index.md` | 首页，使用内联 HTML/CSS 实现卡片式导航。包含三大板块（基础知识 / 绘画流程 / 专题深入）的跳转卡片、课程资源表格、付费课程标签、画前准备提示框 |

#### 基础知识

| 文件 | 作用 |
|:------|:------|
| `docs/人体知识.md` | 人体结构知识：五官比例、头肩颈、腰腹、腿部、手臂与手、区分男女及年龄、五官定位与透视 |
| `docs/场景知识.md` | 场景透视知识：一点/两点/三点透视，使用透视卡片布局展示 |
| `docs/色彩知识.md` | 色彩基础知识：图层混合模式（正片叠底/滤色/叠加） |

#### 绘画流程

按完整作画步骤排列，从起草到最终效果：

| 文件 | 作用 | 状态 |
|:------|:------|:------|
| `流程/起草.md` | 人体动态 → 体块概括 → 骨架肌肉 | 有内容 |
| `流程/勾线.md` | 基础原则、五官处理、线条技巧、图层蒙版 | 有内容 |
| `流程/固有色.md` | 做灰底、固有色选择、特殊处理 | 有内容 |
| `流程/二分.md` | 创建二分图层、初步二分、细化二分、叠加图层 | 有内容 |
| `流程/三分.md` | 腮红、弱结构阴影、眼白眼影、嘴唇、肤色提亮 | 有内容 |
| `流程/闭塞.md` | 闭塞概念（简短） | 有内容 |
| `流程/灰面.md` | 笔刷说明、眼睫毛、眼球、瞳孔、头发画法 | 有内容 |
| `流程/高光.md` | 面部高光、图层混合模式应用表 | 有内容 |
| `流程/反光.md` | — | 空占位 |
| `流程/塑造.md` | — | 空占位 |
| `流程/光源衰减.md` | — | 空占位 |

#### 专题深入

针对特定主题的详细教程：

| 文件 | 作用 |
|:------|:------|
| `专题/眼睛上色.md` | 瞳孔虹膜、颜色反光、高光、发光效果、睫毛处理 |
| `专题/头发塑造.md` | 核心概念、6 步塑造流程、笔刷说明、详细塑造手法、高光反光线稿处理 |
| `专题/皮肤塑造.md` | 整体思路、实操步骤、面部上色流程、五官细化、注意事项 |
| `专题/衣褶.md` | 核心观点、学习路径、取舍思路、4 步刻画流程 |
| `专题/泳装衣褶.md` | 核心难点、刻画技巧、不同区域细化、整体调整 |

### 图片资源

`docs/img/` 目录存放所有图片，共 17 张（PNG/JPG）。图片在 Markdown 中通过 `<img>` 标签引用，使用 `width="X%"` 控制尺寸。

---

## 内容格式规范

所有内容页面遵循统一的格式规范，保持视觉一致性：

### 标题层级

- `#` — 页面标题（每个文件仅一个，自动居中渐变显示）
- `##` — 大板块标题（带左边框 + 背景色条）
- `###` — 子板块标题（带左侧圆点标记）
- 不使用编号标题（如 `## 1. xxx`）

### 列表格式

- 统一使用 `-` 作为列表符号（不用 `*` 或 `(1).`）
- 标签描述格式：`- **标签名**：具体描述内容`
- 子列表缩进两个空格

### 段落与分隔

- 大板块之间用 `---` 分隔线隔开
- 避免大段密集文字，拆分为短句或列表
- 重点内容用 **加粗** 标注

### 图片

- 使用 HTML `<img>` 标签（不用 Markdown `![]()` 语法，方便控制尺寸）
- 格式：`<img src="../img/文件名.png" width="50%" />`
- 根目录文件路径用 `img/`，子目录文件路径用 `../img/`
- 不要使用 Typora 的 `style="zoom: X%"` 属性（浏览器不支持）

### 引用块

- 使用 `>` 创建引用块，用于补充说明、建议、检查提醒

---

## 日常维护流程

### 修改现有内容

1. 找到对应的 `.md` 文件（参考上方目录结构）
2. 按照格式规范编辑内容
3. 本地预览（可选）：`mkdocs serve`，访问 `http://127.0.0.1:8000`
4. 部署上线：`mkdocs gh-deploy --force`

### 新增页面

1. 在 `docs/` 对应目录下创建新的 `.md` 文件
2. 在 `mkdocs.yml` 的 `nav` 部分添加页面入口
3. 如果是首页需要展示，在 `docs/index.md` 中添加对应的卡片链接（注意用 `.html` 后缀）
4. 部署上线

### 新增图片

1. 将图片放入 `docs/img/` 目录
2. 在 Markdown 中引用：`<img src="../img/文件名.png" width="50%" />`
3. 根目录文件用 `img/`，子目录文件用 `../img/`

### 修改样式

- 全局样式在 `docs/stylesheets/extra.css`
- 首页卡片等内联样式在 `docs/index.md` 的 `<style>` 块中
- 主题配置（配色、字体、功能开关）在 `mkdocs.yml` 的 `theme` 部分

### 修改导航结构

- 编辑 `mkdocs.yml` 的 `nav` 部分
- 格式为 `显示名称: 文件路径.md`

---

## 环境搭建（新电脑）

```bash
# 1. 安装 Python 依赖（国内镜像）
pip install mkdocs mkdocs-material -i https://pypi.tuna.tsinghua.edu.cn/simple --trusted-host pypi.tuna.tsinghua.edu.cn

# 2. 克隆仓库
git clone https://github.com/betty-momo/drawing-notes.git
cd drawing-notes

# 3. 本地预览
mkdocs serve

# 4. 部署上线
mkdocs gh-deploy --force
```

---

## 首页卡片结构说明

首页使用内联 HTML 实现卡片导航，结构如下：

```html
<div class="section-title">板块名称</div>

<div class="hero-cards">
    <a class="hero-card" href="路径.html">
        <span class="card-icon">字</span>
        <div class="card-title">标题</div>
        <div class="card-desc">描述</div>
        <span class="card-link">操作 →</span>
    </a>
</div>
```

**注意事项**：

- `href` 必须用 `.html` 后缀（MkDocs 不会自动转换 HTML 标签中的 `.md` 为 `.html`）
- `card-icon` 中放一个汉字作为图标
- `card-desc` 在手机端会自动隐藏以节省空间

---

## 待办事项

- [ ] 补充 `流程/反光.md` 内容
- [ ] 补充 `流程/塑造.md` 内容
- [ ] 补充 `流程/光源衰减.md` 内容
